import { CreateJobs } from "../../Components/CreateJob/CreateJob";
import Layout from "../../Components/Layout/Layout";

const CreateJobsPage = () => {
  return (
    <>
      <CreateJobs />
    </>
  );
};

export default Layout(CreateJobsPage);
